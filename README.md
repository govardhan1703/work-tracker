# projects

- **`backend/`** — Express API + MongoDB (see `backend/.env.example`).
- **`daily-tracker/`** — Vite + React UI (see `daily-tracker/.env.example`).

**Local run:** start `backend` (`npm run dev`), then `daily-tracker` (`npm run dev`).

**Deploy:** follow **[DEPLOY.md](./DEPLOY.md)**.
